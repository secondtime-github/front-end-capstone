const Header = () => {
    return (
        <header>
            Header
            <img
                src="/Logo.svg"
                alt="Logo"
            />
        </header>
    );
}

export default Header;